# Backend origin, used only when the frontend is hosted separately from the API
# (frontend on Vercel, backend on Render).
#
# Set this in the Vercel dashboard under Settings > Environment Variables.
# No trailing slash. Leave it unset for local dev or when the backend serves the
# built frontend itself.
VITE_API_BASE=https://your-service.onrender.com
