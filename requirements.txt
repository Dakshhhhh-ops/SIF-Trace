# SIF-Trace backend dependencies
# Install:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
#
# Pinned to minimum versions verified working together. See requirements-lock.txt
# for the exact resolved versions used during development.

numpy>=2.1
pandas>=2.2.3
scikit-learn>=1.5
scipy>=1.14

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-multipart>=0.0.9

# Notebook (optional - only needed to run notebooks/sif_training.ipynb)
# jupyter>=1.0
# ipykernel>=6.29
# nbconvert>=7.16
